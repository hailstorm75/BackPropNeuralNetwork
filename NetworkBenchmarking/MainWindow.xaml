<Window x:Class="NetworkBenchmarking.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkBenchmarking"     
        mc:Ignorable="d"
        Title="NN Benchmarker" Height="370" Width="525" MinHeight="370" MinWidth="525" Closing="Window_Closing">
  <Window.TaskbarItemInfo>
    <TaskbarItemInfo Description="NN Benchmarker" ProgressState="Normal" ProgressValue="{Binding ProgressTaskBar}"/>
  </Window.TaskbarItemInfo>
  <Grid x:Name="grid">
    <Grid.ColumnDefinitions>
      <ColumnDefinition></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Border Grid.Column="0" BorderThickness="0 0 1 0" BorderBrush="Black">
      <DockPanel x:Name="dockPanel">
        <Slider x:Name="slider" Value="12" Minimum="12" Maximum="24" DockPanel.Dock="Bottom" Margin="10"/>
        <ScrollViewer Margin="10,10,10,10">
          <TextBox DockPanel.Dock="Top" x:Name="textBox" TextWrapping="Wrap" Text="{Binding OutputLog}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="{Binding Value, ElementName=slider}"/>
        </ScrollViewer>
      </DockPanel>
    </Border>
    <Border Grid.Column="1" BorderThickness="1 0 0 0" BorderBrush="Black">
      <StackPanel>
        <!-- Iterations -->
        <DockPanel>
          <Label DockPanel.Dock="Left" Content="Max iterations:"/>
          <Label DockPanel.Dock="Right" Content="{Binding Value, ElementName=sld_iterations}"/>
        </DockPanel>
        <Slider Name="sld_iterations" Margin="10, 0, 10, 0" Minimum="5" Maximum="25" Value="{Binding InputIterations, Mode=TwoWay}" IsSnapToTickEnabled="True" TickFrequency="5"/>
        <!-- Network hidden -->
        <Label Content="Hidden layer neurons min/max:"/>
        <Grid Margin="10, 0, 10, 5">
          <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="10" />
            <ColumnDefinition/>
          </Grid.ColumnDefinitions>
          <TextBox Name="txt_min" Text="{Binding InputMinimum, Mode=TwoWay}" TextAlignment="Center" Padding="2" Grid.Column="0"></TextBox>
          <TextBox Name="txt_max" Text="{Binding InputMaximum, Mode=TwoWay}" TextAlignment="Center" Padding="2" Grid.Column="2"></TextBox>
        </Grid>
        <!-- Dataset type -->
        <TabControl IsSynchronizedWithCurrentItem="True" Margin="10">
          <TabItem Header="Generated">
            <StackPanel>
              <!-- Network IO -->
              <Label Content="Network I/O count:"/>
              <Grid Margin="10, 0, 10, 5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition/>
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Name="txt_inputs" Text="{Binding InputInputs, Mode=TwoWay}" TextAlignment="Center" Padding="2" Grid.Column="0"></TextBox>
                <TextBox Name="txt_outputs" Text="{Binding InputOutputs, Mode=TwoWay}" TextAlignment="Center" Padding="2" Grid.Column="2"></TextBox>
              </Grid>
              <Label Content="Generate test data with:"/>
              <ComboBox x:Name="cmb_DataType" Margin="10, 0, 10, 10" SelectedIndex="0" SelectedItem="{Binding OperationType}" ItemsSource="{Binding OperationTypes}"/>
            </StackPanel>
          </TabItem>
          <TabItem Header="Loaded"></TabItem>
        </TabControl>
        <DockPanel Margin="10, 5, 10, 10" >
          <Button DockPanel.Dock="Right" x:Name="btn_Cancel" Content="Cancel" Command="{Binding CancelOperationCommand}" IsEnabled="{Binding ButtonCancelEnabled, Mode=OneWay}" Margin="5,0,0,0"/>
          <Button DockPanel.Dock="Left" x:Name="btn_Start" Content="Start" Command="{Binding RunBenchmarkCommand, IsAsync=True}" IsEnabled="{Binding ButtonStartEnabled, Mode=OneWay}" Margin="0,0,5,0"/>
        </DockPanel>
        <Grid>
          <ProgressBar Name="prg_Progress" Height="20" Margin="10, 0, 10, 0" Value="{Binding Progress, Mode=OneWay}"/>
          <TextBlock Text="{Binding ElementName=prg_Progress, Path=Value, StringFormat={}{0:0}%}" 
           HorizontalAlignment="Center" 
           VerticalAlignment="Center" />
        </Grid>
      </StackPanel>
    </Border>
  </Grid>
</Window>
